{
  "schemaVersion": 1,
  "id": "serverpropertiesreload",
  "name": "${name}",
  "description": "${description}",
  "version": "${version}",
  "license": "${license}",
  
  "authors": [
    "${author}"
  ],
  
  "contact": {
    "issues": "${issuesURL}",
    "sources": "${sourcesURL}"
  },
  
  "environment": "server",
  "depends": {
    "fabricloader": ">=0.19.3",
    "fabric-api": ">=0.152.1",
    "minecraft": "~26.2",
    "java": ">=25"
  },
  "mixins": [
    "serverpropertiesreload.mixins.json"
  ]
}
